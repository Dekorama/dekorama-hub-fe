[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
  # Netlify UI (required):
  # API_PROXY_TARGET=https://dekorama-hub-be.onrender.com
  # Do NOT set NEXT_PUBLIC_API_BASE_URL to onrender — breaks iOS cookies.

[[plugins]]
  package = "@netlify/plugin-nextjs"
